# Server Configuration
PORT=3000

# Optional: Custom log level (error, warn, info, debug)
LOG_LEVEL=info

# Optional: Custom auth folder name
AUTH_FOLDER=auth_info_baileys

# Optional: Rate limiting (requests per window)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000

# Optional: File upload size limit in bytes (default: 100MB)
MAX_FILE_SIZE=104857600 